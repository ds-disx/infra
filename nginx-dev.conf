events {
}

http {
    server {
        listen 8080;

        location /posts {
            proxy_pass http://localhost:3010/posts;
        }

        location /comments {
            proxy_pass http://localhost:3020/comments;
        }    
    }
}
